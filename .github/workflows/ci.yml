name: CI - Sprint 3 (CI/CD)

on:
  push:
    branches: [main, 'feat/**']
  pull_request:
    branches: [main]

jobs:
  # ------------------------------------
  # JOB 1 - Frontend Angular
  # ------------------------------------
  frontend:
    name: Frontend (Angular 17)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: frontend/node_modules
          key: node-modules-${{ hashFiles('frontend/package.json') }}
          restore-keys: node-modules-

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi

      - name: Build (production)
        run: npm run build -- --configuration production

      - name: Unit tests (headless) + coverage
        run: npm run test -- --watch=false --browsers=ChromeHeadless --code-coverage

      - name: "[S2-A] auth.interceptor.ts unico (sem duplicata)"
        run: |
          COUNT=$(find src -name 'auth.interceptor.ts' | wc -l)
          [ "$COUNT" -eq 1 ] \
            && echo "auth.interceptor.ts sem duplicata OK" \
            || (echo "Duplicata encontrada: $COUNT arquivos" && exit 1)

      - name: "[S2-A] GlobalErrorHandler registrado no AppModule"
        run: |
          grep -q 'GlobalErrorHandler' src/app/app.module.ts \
            || grep -rq 'GlobalErrorHandler' src/app/app.config.ts 2>/dev/null \
            && echo "GlobalErrorHandler registrado OK" \
            || (echo "GlobalErrorHandler nao registrado" && exit 1)

      - name: "[S2-A] interceptor trata 401/403"
        run: |
          grep -q '401' src/app/features/auth/auth.interceptor.ts \
            && grep -q '403' src/app/features/auth/auth.interceptor.ts \
            && echo "auth.interceptor OK" \
            || (echo "Tratamento 401/403 ausente" && exit 1)

      - name: "[S2-A] componentes UX polish existem"
        run: |
          for f in \
            src/app/core/components/loading-skeleton.component.ts \
            src/app/core/components/empty-state.component.ts \
            src/app/core/components/error-banner.component.ts \
            src/app/core/handlers/global-error-handler.ts; do
            [ -f "$f" ] || (echo "Ausente: $f" && exit 1)
          done
          echo "Componentes UX polish OK"

      - name: "[S2-A] adminGuard nas rotas de training"
        run: |
          grep -q 'adminGuard' src/app/features/training-dashboard/training-dashboard.routes.ts \
            && echo "adminGuard presente" \
            || (echo "adminGuard ausente" && exit 1)

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: frontend-coverage
          path: frontend/coverage/

  # ------------------------------------
  # JOB 2 - Backend Rust
  # ------------------------------------
  backend:
    name: Backend (Rust)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: btv
          POSTGRES_PASSWORD: btv
          POSTGRES_DB: btvtest
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      DATABASE_URL: postgres://btv:btv@localhost:5432/btvtest
      JWT_SECRET: test_secret_for_ci_only
      UPLOAD_DIR: /tmp/btv-uploads
      OLLAMA_URL: http://localhost:11434
      OLLAMA_MODEL: llama3.1:8b
      OLLAMA_MOCK: "true"
      TRAINING_MOCK: "true"
      TRAINING_URL: https://api.buildtovalue.cloud
      QDRANT_URL: http://localhost:6333
      EMBEDDING_URL: http://localhost:8001

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            backend/target
          key: ${{ runner.os }}-cargo-backend-${{ hashFiles('backend/Cargo.toml', 'backend/crates/api/Cargo.toml', 'backend/crates/document-processor/Cargo.toml') }}
          restore-keys: ${{ runner.os }}-cargo-backend-

      - name: cargo fmt
        run: cargo fmt --all -- --check

      - name: cargo clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Criar diretorio de uploads
        run: mkdir -p $UPLOAD_DIR

      - name: Run migrations
        run: |
          cargo install sqlx-cli --no-default-features --features postgres 2>/dev/null || true
          sqlx migrate run --database-url "$DATABASE_URL" --source migrations

      - name: "[S2-B] AdminService extraido existe"
        run: |
          [ -f crates/api/src/services/admin_service.rs ] \
            && echo "admin_service.rs OK" \
            || (echo "admin_service.rs ausente" && exit 1)

      - name: "[S2-B] UserService extraido existe"
        run: |
          [ -f crates/api/src/services/user_service.rs ] \
            && echo "user_service.rs OK" \
            || (echo "user_service.rs ausente" && exit 1)

      - name: "[S2-B] AuditService extraido existe"
        run: |
          [ -f crates/api/src/services/audit_service.rs ] \
            && echo "audit_service.rs OK" \
            || (echo "audit_service.rs ausente" && exit 1)

      - name: "[S2-B] AppState usa servicos extraidos"
        run: |
          grep -q 'admin_service' crates/api/src/state.rs \
            && grep -q 'user_service' crates/api/src/state.rs \
            && grep -q 'audit_service' crates/api/src/state.rs \
            && echo "AppState com servicos OK" \
            || (echo "AppState sem servicos extraidos" && exit 1)

      - name: "[S2-B] admin.rs delega para AdminService (SRP)"
        run: |
          grep -q 'admin_service' crates/api/src/routes/admin.rs \
            && echo "admin.rs delega para AdminService OK" \
            || (echo "admin.rs nao delega para AdminService" && exit 1)

      - name: "[S1-C] Migration refresh_tokens existe"
        run: |
          ls migrations/ | grep -q 'refresh_tokens' \
            && echo "Migration refresh_tokens presente" \
            || (echo "Migration refresh_tokens ausente" && exit 1)

      - name: "[S1-C] AppConfig centralizado existe"
        run: |
          [ -f crates/api/src/config.rs ] \
            && echo "config.rs OK" \
            || (echo "config.rs ausente" && exit 1)

      - name: "[RAG] Arquivos RAG worker existem"
        run: |
          for f in \
            crates/document-processor/src/config.rs \
            crates/document-processor/src/extractor.rs \
            crates/document-processor/src/strategy.rs \
            crates/document-processor/src/chunker.rs \
            crates/document-processor/src/embedder.rs \
            crates/document-processor/src/indexer.rs \
            crates/api/src/rag.rs; do
            [ -f "$f" ] || (echo "Ausente: $f" && exit 1)
          done
          echo "Todos os arquivos RAG presentes"

      - name: "[RAG] Testes unitarios - strategy"
        run: cargo test -p document-processor strategy_tests -- --test-threads=1 --nocapture

      - name: "[RAG] Testes unitarios - chunker"
        run: cargo test -p document-processor chunker_tests -- --test-threads=1 --nocapture

      - name: "[RAG] Testes unitarios - extractor"
        run: cargo test -p document-processor extractor_tests -- --test-threads=1 --nocapture

      - name: "[RAG] Testes unitarios - rag.rs"
        run: cargo test -p api rag_unit_tests -- --test-threads=1 --nocapture

      - name: "[RAG] Testes de integracao - chat com RAG graceful degradation"
        run: cargo test -p api rag_integration_tests -- --test-threads=1 --nocapture

      - name: cargo test (suite completa)
        run: cargo test --all -- --test-threads=1

      - name: "[S1-A] Middleware JWT presente"
        run: |
          grep -rq 'jwt' crates/api/src/middleware/ \
            && echo "Middleware JWT OK" \
            || (echo "Middleware JWT ausente" && exit 1)

  # ------------------------------------
  # JOB 3 - Smoke test
  # ------------------------------------
  integration-smoke:
    name: Integration smoke test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: btv
          POSTGRES_PASSWORD: btv
          POSTGRES_DB: btvsmoke
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      DATABASE_URL: postgres://btv:btv@localhost:5432/btvsmoke
      JWT_SECRET: test_secret_for_ci_only
      UPLOAD_DIR: /tmp/btv-uploads
      OLLAMA_URL: http://localhost:11434
      OLLAMA_MODEL: llama3.1:8b
      OLLAMA_MOCK: "true"
      TRAINING_MOCK: "true"
      TRAINING_URL: https://api.buildtovalue.cloud
      QDRANT_URL: http://localhost:6333
      EMBEDDING_URL: http://localhost:8001

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo (smoke-only)
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            backend/target
          key: ${{ runner.os }}-cargo-smoke-${{ hashFiles('backend/Cargo.toml', 'backend/crates/api/Cargo.toml') }}
          restore-keys: ${{ runner.os }}-cargo-smoke-

      - name: Install sqlx-cli
        run: cargo install sqlx-cli --no-default-features --features postgres 2>/dev/null || true

      - name: Run migrations
        working-directory: backend
        run: sqlx migrate run --database-url "$DATABASE_URL" --source migrations

      - name: Build backend (debug)
        working-directory: backend
        run: cargo build -p api

      - name: Start backend
        working-directory: backend
        run: |
          mkdir -p $UPLOAD_DIR
          ./target/debug/api &
          echo $! > /tmp/api.pid
          for i in $(seq 1 40); do
            if curl -sf http://localhost:3000/health > /dev/null 2>&1; then
              echo "Backend respondeu na tentativa $i"
              break
            fi
            [ "$i" -eq 40 ] && echo "Backend nao respondeu" && exit 1
            echo "Aguardando... ($i/40)" && sleep 3
          done

      - name: "[Smoke] GET /health"
        run: |
          R=$(curl -sf http://localhost:3000/health)
          echo "$R" | grep -q '"status"' || (echo "/health sem status" && exit 1)
          echo "/health OK"

      - name: "[Smoke] POST /auth/register"
        run: |
          CODE=$(curl -s -o /dev/null -w '%{http_code}' \
            -X POST http://localhost:3000/api/v1/auth/register \
            -H 'Content-Type: application/json' \
            -d '{"workspace_name":"Smoke WS","name":"Smoke","email":"smoke@test.com","password":"Smoke@1234"}')
          [ "$CODE" = "201" ] || [ "$CODE" = "409" ] \
            || (echo "/auth/register retornou $CODE" && exit 1)
          echo "/auth/register OK ($CODE)"

      - name: "[Smoke] POST /auth/login retorna token"
        run: |
          curl -s -X POST http://localhost:3000/api/v1/auth/register \
            -H 'Content-Type: application/json' \
            -d '{"workspace_name":"Smoke WS2","name":"Smoke2","email":"smoke2@test.com","password":"Smoke@1234"}' > /dev/null || true
          R=$(curl -sf -X POST http://localhost:3000/api/v1/auth/login \
            -H 'Content-Type: application/json' \
            -d '{"email":"smoke2@test.com","password":"Smoke@1234"}')
          echo "$R" | grep -q '"token"' || (echo "/auth/login sem token" && exit 1)
          echo "$R" > /tmp/login_resp.json
          TOKEN=$(python3 .github/scripts/extract_json_field.py token < /tmp/login_resp.json)
          echo "TOKEN=$TOKEN" >> $GITHUB_ENV
          echo "/auth/login OK"

      - name: "[Smoke] POST /auth/refresh retorna novo token"
        run: |
          REFRESH=$(python3 .github/scripts/extract_json_field.py refresh_token < /tmp/login_resp.json || echo "")
          if [ -n "$REFRESH" ]; then
            R=$(curl -sf -X POST http://localhost:3000/api/v1/auth/refresh \
              -H 'Content-Type: application/json' \
              -d "{\"refresh_token\":\"$REFRESH\"}" || echo "skip")
            echo "$R" | grep -q '"token"' \
              && echo "/auth/refresh OK" \
              || echo "/auth/refresh sem token (aceitavel se nao implementado)"
          else
            echo "/auth/refresh: refresh_token ausente no login (skip)"
          fi

      - name: "[Smoke RAG] Chat sem Qdrant nao quebra"
        run: |
          curl -sf -X POST http://localhost:3000/api/v1/chats \
            -H "Authorization: Bearer $TOKEN" \
            -H 'Content-Type: application/json' \
            -d '{"title": "Smoke RAG Test"}' > /tmp/chat_resp.json
          CHAT_ID=$(python3 .github/scripts/extract_json_field.py id < /tmp/chat_resp.json)
          echo "Chat ID: $CHAT_ID"

          curl -sf -X POST "http://localhost:3000/api/v1/chats/$CHAT_ID/messages" \
            -H "Authorization: Bearer $TOKEN" \
            -H 'Content-Type: application/json' \
            -d '{"content": "Teste de RAG sem Qdrant"}' > /tmp/msg_resp.json
          cat /tmp/msg_resp.json

          grep -q '"role"' /tmp/msg_resp.json || (echo "Resposta sem role" && exit 1)
          python3 .github/scripts/check_rag_response.py /tmp/msg_resp.json

      - name: "[Smoke] /training sem token retorna 401/403"
        run: |
          CODE=$(curl -s -o /dev/null -w '%{http_code}' \
            http://localhost:3000/api/v1/training/queue)
          [ "$CODE" = "401" ] || [ "$CODE" = "403" ] \
            || (echo "Rota protegida retornou $CODE" && exit 1)
          echo "/training protegida OK"

      - name: "[Smoke S2-B] /admin/health retorna 200 ou 401"
        run: |
          CODE=$(curl -s -o /dev/null -w '%{http_code}' \
            http://localhost:3000/api/v1/admin/health)
          [ "$CODE" = "200" ] || [ "$CODE" = "401" ] || [ "$CODE" = "403" ] \
            || (echo "/admin/health retornou $CODE" && exit 1)
          echo "/admin/health OK ($CODE)"

      - name: Stop backend
        if: always()
        run: kill $(cat /tmp/api.pid) 2>/dev/null || true

  # ------------------------------------
  # JOB 4 - Gate final
  # ------------------------------------
  all-green:
    name: All checks passed
    runs-on: ubuntu-latest
    needs: [frontend, backend, integration-smoke]
    if: always()
    steps:
      - name: Verify all jobs succeeded
        run: |
          if [ "${{ needs.frontend.result }}" != "success" ] || \
             [ "${{ needs.backend.result }}" != "success" ] || \
             [ "${{ needs.integration-smoke.result }}" != "success" ]; then
            echo "One or more jobs failed."
            echo "  Frontend:          ${{ needs.frontend.result }}"
            echo "  Backend:           ${{ needs.backend.result }}"
            echo "  Integration smoke: ${{ needs.integration-smoke.result }}"
            exit 1
          fi
          echo "All checks passed - Sprint 3 CI/CD OK"
