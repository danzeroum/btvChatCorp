// REMOVIDO (S2-A / F3): este arquivo era duplicata da versão funcional auth.interceptor.fn.ts
// Use auth.interceptor.fn.ts — registrado em app.config.ts via withInterceptors([authInterceptorFn])
// Este stub existe apenas para evitar erros de import em código legado ainda não migrado.
// TODO: remover após verificar zero referências com `grep -r auth.interceptor.ts src/`
export {};
